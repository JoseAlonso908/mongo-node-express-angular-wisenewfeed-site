<!DOCTYPE html>
<html>
	<head>
		<link rel="icon" type="image/x-icon" href="/assets/images/favicon.ico" />
		<title>{{user.name}} - {{user.title}} | Wisenewsfeed</title>
		<meta http-equiv="content-type" content="text/html; charset=UTF-8" />
		<meta name="robots" content="index,follow" />
		<meta name="description" content="{{user.intro}}" />
	</head>
	<body>
		<h1>{{user.name}} - {{user.title}}</h1>
		<h2>Badge - {{user.xpInfo.badge}}</h2>
		<h2>Level - {{user.xpInfo.level}}</h2>
		<p>{{user.intro}}</p>
		<ul>
			{{#each articles}}
			<li><a href="https://sandboxapp.wisenewsfeed.com/#!/article/{{_id}}">{{title}}</a></li>
			{{/each}}
		</ul>
	</body>
</html>
